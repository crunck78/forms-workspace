import { NgxValidatorsDirective } from './ngx-validators.directive';

describe('NgxValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
