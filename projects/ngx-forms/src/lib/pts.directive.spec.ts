import { PtsDirective } from './pts.directive';

describe('PtsDirective', () => {
  it('should create an instance', () => {
    const directive = new PtsDirective();
    expect(directive).toBeTruthy();
  });
});
