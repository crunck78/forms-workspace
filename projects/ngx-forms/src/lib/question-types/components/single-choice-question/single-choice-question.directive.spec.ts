import { SingleChoiceQuestionDirective } from './single-choice-question.directive';

describe('SingleChoiceQuestionDirective', () => {
  it('should create an instance', () => {
    const directive = new SingleChoiceQuestionDirective();
    expect(directive).toBeTruthy();
  });
});
