import { NgxFormsDirective } from './ngx-forms.directive';

describe('NgxFormsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFormsDirective();
    expect(directive).toBeTruthy();
  });
});
